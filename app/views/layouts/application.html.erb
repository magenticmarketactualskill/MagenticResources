<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title><%= content_for(:title) || "MagenticResources" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="MagenticResources">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="h-full">
    <% if user_signed_in? %>
      <div class="min-h-full">
        <nav class="bg-indigo-600">
          <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="flex h-16 items-center justify-between">
              <div class="flex items-center">
                <div class="shrink-0">
                  <%= link_to root_path, class: "text-white font-bold text-xl" do %>
                    MagenticResources
                  <% end %>
                </div>
                <div class="hidden md:block">
                  <div class="ml-10 flex items-baseline space-x-4">
                    <%= link_to "Dashboard", root_path, class: "rounded-md px-3 py-2 text-sm font-medium #{request.path == root_path ? 'bg-indigo-700 text-white' : 'text-indigo-200 hover:bg-indigo-500 hover:text-white'}" %>
                    <%= link_to "Resources", resources_path, class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/resources') ? 'bg-indigo-700 text-white' : 'text-indigo-200 hover:bg-indigo-500 hover:text-white'}" %>
                    <%= link_to "Skills", skills_path, class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/skills') ? 'bg-indigo-700 text-white' : 'text-indigo-200 hover:bg-indigo-500 hover:text-white'}" %>
                    <%= link_to "Teams", teams_path, class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/teams') ? 'bg-indigo-700 text-white' : 'text-indigo-200 hover:bg-indigo-500 hover:text-white'}" %>
                    <%= link_to "Projects", projects_path, class: "rounded-md px-3 py-2 text-sm font-medium #{request.path.start_with?('/projects') ? 'bg-indigo-700 text-white' : 'text-indigo-200 hover:bg-indigo-500 hover:text-white'}" %>
                  </div>
                </div>
              </div>
              <div class="hidden md:block">
                <div class="ml-4 flex items-center md:ml-6">
                  <span class="text-indigo-200 text-sm mr-4"><%= current_user.display_name %></span>
                  <%= button_to "Sign out", logout_path, method: :delete, class: "rounded-md bg-indigo-500 px-3 py-2 text-sm font-medium text-white hover:bg-indigo-400" %>
                </div>
              </div>
            </div>
          </div>
        </nav>

        <% if flash[:notice] %>
          <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
            <div class="rounded-md bg-green-50 p-4">
              <div class="flex">
                <div class="shrink-0">
                  <svg class="size-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd" />
                  </svg>
                </div>
                <div class="ml-3">
                  <p class="text-sm font-medium text-green-800"><%= flash[:notice] %></p>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <% if flash[:alert] %>
          <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
            <div class="rounded-md bg-red-50 p-4">
              <div class="flex">
                <div class="shrink-0">
                  <svg class="size-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd" />
                  </svg>
                </div>
                <div class="ml-3">
                  <p class="text-sm font-medium text-red-800"><%= flash[:alert] %></p>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <main>
          <div class="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
            <%= yield %>
          </div>
        </main>
      </div>
    <% else %>
      <%= yield %>
    <% end %>
  </body>
</html>
